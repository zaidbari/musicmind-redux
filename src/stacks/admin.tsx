import { ReactElement } from 'react'
import { Text, View } from 'react-native'

const AdminStack = (): ReactElement => {
	return (
		<View>
			<Text>Admin Stack</Text>
		</View>
	)
}

export default AdminStack
